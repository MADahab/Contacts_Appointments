import React from "react";

{/* export const ContactPicker = ({contacts, setContact}) => {

  
  
  return (
    <select onChange={handleContact} name="contact" >
      <option value="" selected disabled hidden>no contact is selected</option>
      {contacts.map((contact,index) => {
        return (
          <option 
            value={contact.name} 
            key={index}
            contact={contact.name}
            >
            {contact.name}
          </option>
        );
      })}         
    </select>
  );
};
*/}


